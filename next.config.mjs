// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  outputFileTracingRoot: __dirname,
  // distDir: '.next', // default, can remove this line
};

export default nextConfig;

